# Korrepetitor AI (OnlyHuman.hu)
Flask-alapú tanulói chatfelület az OpenAI API segítségével.

## Telepítés
1. Másold le a repót
2. Hozz létre `.env` fájlt a `.env.example` alapján
3. Telepítsd a csomagokat:
```bash
pip install -r requirements.txt
